# Messenger Webhook -- node.js

After pull code from brand _master_, Please run following these command below:

1. `npm install`
2. `npm start`

The server should be installed latest of node version. 